<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.domain.user.mapper.command.UserCommandMapper">

    <insert id="save" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (name, email, password, profile_image_url, social_id, role, created_at, updated_at)
        VALUES (#{name}, #{email}, #{password}, #{profileImageUrl}, #{socialId}, #{role}, NOW(), NOW())
    </insert>
    <update id="register">
        UPDATE users
        SET nick_name  = #{reqDto.nickname},
            role       = #{reqDto.role},
            updated_at = now()
        WHERE id = #{userId}
    </update>


</mapper>
