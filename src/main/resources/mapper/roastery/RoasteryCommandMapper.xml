<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.backend.domain.roastery.mapper.command.RoasteryCommandMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roasteries (, name, logo_url, website_url, created_at, updated_at)
        VALUES (#{ownerId}, #{name}, #{logoUrl}, #{websiteUrl}, NOW(), NOW())
    </insert>
    
    <update id="update">
        UPDATE roasteries
        SET name        = #{name},
            logo_url    = #{logoUrl},
            website_url = #{websiteUrl},
            updated_at  = NOW()
        WHERE id = #{id}
    </update>
</mapper>
