<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.domain.user.mapper.query.UserQueryMapper">

    <select id="findById" parameterType="long" resultType="User">
        SELECT *
        FROM user
        WHERE id = #{userId}
    </select>

    <select id="findByEmail" parameterType="string" resultType="User">
        SELECT *
        FROM user
        WHERE email = #{email}
    </select>

    <select id="findBySocialId" parameterType="string" resultType="User">
        SELECT *
        FROM user
        WHERE social_id = #{socialId}
    </select>

    <select id="existByNickname" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE nick_name = #{nickname}
    </select>

    <select id="existBySocialId" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE social_id = #{socialId}
    </select>

</mapper>
